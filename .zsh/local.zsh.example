# Copy to ~/.zsh/local.zsh for machine- or work-specific settings.
# Keep this file out of shared/public dotfiles.

# Examples:
# AWS configuration is usually best managed in ~/.aws/config.
# If a machine-wide default is needed, set it here with generic values:
# export AWS_PROFILE=your-profile
# export AWS_REGION=your-region
#
# Put any other machine-specific paths or environment variables here.
